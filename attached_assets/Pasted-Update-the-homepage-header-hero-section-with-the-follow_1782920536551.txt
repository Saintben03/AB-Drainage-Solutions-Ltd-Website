Update the homepage header/hero section with the following changes. Do NOT remove the top "24/7 Emergency" scrolling banner — the owner wants this kept as-is.

Consolidate the contact bar below the emergency banner: Currently it shows two phone numbers (01256 688 650 and 07498 062 710) plus email and social icons. Simplify to show only ONE phone number here — the office line (01256 688 650) — plus email and social icons. Remove the second mobile number from this bar entirely.
Fix duplicate phone numbers across the nav: The main nav currently shows "Emergency Response 07498 062 710" as a separate call-to-action next to the "Book Now" button. Remove this from the nav — keep only one phone CTA in the header area, ideally the "Book Now" button on its own, since the number already appears in the top contact bar.
Reduce phone number repetition in the hero section: The hero has a "CALL 01256 688 650" button next to "OUR SERVICES". Keep this — it's the single primary emergency CTA for the hero — but make sure no other number appears again in this section.
Improve text readability over the hero image: Add a stronger dark gradient overlay specifically behind the headline/paragraph text block (left ~55% of the hero image), rather than a flat overlay across the whole image, so the truck/hose details in the photo don't compete visually with the text.
Smooth the section transitions below the hero: Currently there's a hard cut from the dark hero straight into a solid blue "Need drainage support" band, then a light wave curve into a white section — three background treatments stacked closely. Simplify to two transitions: hero (dark) → single blue CTA band with the existing wave curve into the white section below. Remove any extra abrupt colour block in between if one exists in the current markup.
Keep all existing content, copy, trust badges (33 years, 1 hour ETA, Hampshire wide, star rating), and both CTA buttons — this is about reducing visual noise and repeated phone numbers, not removing content.
